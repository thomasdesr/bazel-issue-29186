load("@rules_shell//shell:sh_test.bzl", "sh_test")

genrule(
    name = "gen60",
    outs = ["out60.txt"],
    cmd = "seq 1 500 > $@",
)

sh_test(
    name = "test60",
    srcs = ["//pkg:t.sh"],
    data = [":gen60"],
)
